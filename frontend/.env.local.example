# Optional: TastyAgent Frontend Environment Variables
# Copy to frontend/.env.local if you need to point to a remote or custom backend API.
# Default if unset: http://localhost:8000
NEXT_PUBLIC_API_BASE=http://localhost:8000
